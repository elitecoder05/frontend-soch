# Frontend Environment Configuration for Vercel/Railway Deployment

# Primary backend URL (Railway)
VITE_API_BASE_URL=https://backend-soch-production-56cd.up.railway.app

# Fallback URL (alternative Railway app)
VITE_API_FALLBACK_URL=https://backend-soch-production-b526.up.railway.app

# For future custom domain setup:
# VITE_API_BASE_URL=https://api.sochai.store
# VITE_API_FALLBACK_URL=https://backend-soch-production-56cd.up.railway.app

# Note: These environment variables will override the hardcoded URLs in src/api/index.ts
# Make sure to set these in your Vercel dashboard under Project Settings > Environment Variables